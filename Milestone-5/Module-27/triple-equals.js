console.log(2 === 2);
console.log(2 === 3);
console.log(2 === '2');

// === Compares the data types first. If data types are different it returns false. If data types are same, it compares the values and returns true or false accordingly.