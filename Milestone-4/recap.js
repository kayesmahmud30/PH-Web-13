/*
1. How to use js with html and css.
2. Get elements from DOM
    2-1. getElementById
    2-2. getElementsByClassName
    2-3. querySelector
    2-4. querySelectorAll

3. Dynamically add elements to the DOM: document.createElement
    3:5. Set value to the innerHTML

4. Get and set value from/to a DOM elements.
    4-1. Use innerText (h1, p, h5, div, span, strong)
    4-2: Use value (input, textArea)

5. Add event listener (Event handler)
    5-1. click, focus, blur, on change, keyup
    5-2. addEventListener onclick = "myHandler()"

6. Use function to reduce duplicate code.

7. Event bubble and Event delegate.

*/

document.querySelectorAll(".usersContainer .disable h3");
